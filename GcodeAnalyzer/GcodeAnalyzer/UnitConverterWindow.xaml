<Window x:Class="GcodeAnalyzer.UnitConverterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GcodeAnalyzer"
        mc:Ignorable="d"
        Title="Unit Converter - Inches to Millimeters" 
        Height="340" Width="400" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF1E1E1E">
    
    <Window.Resources>
        <!-- Dark theme colors -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FF3F3F46"/>
        <SolidColorBrush x:Key="ForegroundBrush" Color="#FFE0E0E0"/>
        <SolidColorBrush x:Key="ForegroundDimBrush" Color="#FF9E9E9E"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF007ACC"/>
        
        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF1C97EA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        
        <!-- Label Style -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Convert Inches to Millimeters" 
                   FontSize="16" FontWeight="Bold" 
                   Foreground="#FF00D4FF" Margin="0,0,0,20"/>
        
        <!-- Inches Input -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Inches:" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtInches" 
                     TextChanged="TxtInches_TextChanged"
                     PreviewTextInput="TxtInches_PreviewTextInput"/>
        </StackPanel>
        
        <!-- Millimeters Output -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Millimeters:" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtMillimeters" 
                     IsReadOnly="True"
                     Background="#FF252526"
                     FontWeight="Bold"
                     Foreground="#FF00D4FF"/>
        </StackPanel>
        
        <!-- Info Text -->
        <TextBlock Grid.Row="3" 
                   Text="1 inch = 25.4 millimeters" 
                   Foreground="{StaticResource ForegroundDimBrush}"
                   FontSize="12"
                   VerticalAlignment="Top"
                   Margin="0,5,0,0"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Place" Click="BtnPlace_Click" 
                    Width="100" Margin="0,0,10,0"
                    ToolTip="Place millimeter value at cursor position"/>
            <Button Content="Cancel" Click="BtnCancel_Click" 
                    Width="100"
                    Background="#FF3E3E42"/>
        </StackPanel>
    </Grid>
    
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="ApplicationCommands.Close"/>
        <KeyBinding Key="Enter" Command="ApplicationCommands.Properties"/>
    </Window.InputBindings>
    
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Close" Executed="CloseCommand_Executed"/>
        <CommandBinding Command="ApplicationCommands.Properties" Executed="PlaceCommand_Executed"/>
    </Window.CommandBindings>
</Window>
